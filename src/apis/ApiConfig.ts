export const HOST_SERVER = 'http://localhost:8080'

export const ApiConfig = {
    HOST_SERVER: HOST_SERVER,
    AUTH_API: `${HOST_SERVER}` + `/api/auth/login`,
    BLOG_API: `${HOST_SERVER}` + `/api/blog`,
    BLOG_COMMENT_API: `${HOST_SERVER}` + `/api/blog-comment`
}
